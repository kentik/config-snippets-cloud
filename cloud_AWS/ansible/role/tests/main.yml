- hosts: 127.0.0.1
  connection: local
  gather_facts: false
  roles:
  - role: test_vpcs
  - role: "{{ playbook_dir }}/../"
    vars:
      vpc_id_list: "{{ single_vpc_ids }}"
  - role: "{{ playbook_dir }}/../"
    vars:
      vpc_id_list: "{{ multi_vpc_ids }}"
